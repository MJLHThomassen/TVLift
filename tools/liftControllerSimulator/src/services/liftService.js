liftService = {
    status: "offline",
    speed: 100,
    settings: {
        version: 1,
        liftMinSpeed: 5,
        liftMaxSpeed: 8000,
        liftDefaultSpeed: 40,
    }
}

module.exports = liftService;